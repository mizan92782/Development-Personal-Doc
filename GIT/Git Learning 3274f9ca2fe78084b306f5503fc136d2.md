# Git Learning

![image.png](Git%20Learning/image.png)

![image.png](Git%20Learning/image%201.png)

```
  ┌────────────────────┐
  │  Working Directory │
  │ (edit code here)   │
  └─────────┬──────────┘
            │
            │ git add
            ▼
  ┌────────────────────┐
  │  Staging Area      │
  │ (Index)            │
  └─────────┬──────────┘
            │
            │ git commit
            ▼
  ┌────────────────────┐
  │ Local Repository   │
  │ (commit history)   │
  └─────────┬──────────┘
            │
            │ git push
            ▼
  ┌────────────────────┐
  │ Remote Repository  │
  │ (GitHub, GitLab)   │
  └────────────────────┘
```

## Tracked and UnTracked File

![image.png](Git%20Learning/image%202.png)

## Git add Difference

![image.png](Git%20Learning/image%203.png)

## Git Commit history

```jsx
>> git log
```

Git commit store a snapshot of changing of current state of code with metadata like Auther ,message,time etc

![image.png](Git%20Learning/image%204.png)

## **Amending a commit**

![image.png](Git%20Learning/image%205.png)

## Multiple commit message

```jsx
git commit -m "message_1" -m "message_2" 
```

## **Committing a Specific File**

![image.png](Git%20Learning/image%206.png)

## Git Remote management

```jsx
### Connect with remote repo
>> git remote add origin <URL>

### View current connect repo
>> git remote  -v

### Rename Remote repe name (local)
>> git remote rename origin <newname>

### Disconnet a Remote
>> git remote remove <repositoryname>

### Show remote details
>>> git remote show origin

### Clone repo
>> git clone <remote-repository-URL>

### Fetch Repo
>> git fetch <remote-name>

### Pull (fetch+ merge in to local) Changes from Remote
>> git pull <remote-name> <branch-name>>

## Push to remote
>> git push <remote-name> <branch-name>

##=============== branched ==================================

### remote all branches
>> git brach -r

### delete a remot bransh
>> git push origin --delete <branch-name>

#======================= Authetication=====================
 key generation >>           ssh-keygen -t rsa -b 4096 -C "your-email@example.com"
 ssh agent start >>           eval "$(ssh-agent -s)"
 add privete key in local >>  ssh-add ~/.ssh/id_rsa
 show privete key>>           cat ~/.ssh/id_rsa.pub
 sheck ssh test >>            ssh -T git@github.com
 add in github public key..............
 
 
```

## Why  repo name is ‘origin”

```jsx
mizan@mizan-Inspiron-3501:~/Desktop/Git-Learning$ git remote -v
origin  git@github.com:mizan92782/Git-Learning.git (fetch)
origin  git@github.com:mizan92782/Git-Learning.git (push)

Remote name “origin” কেন?
Git-এ remote বলতে বোঝায় অন্য repository, যেমন GitHub বা GitLab এ যে repo থাকে।
যখন আপনি প্রথমবার কোনো remote add করেন (যেমন GitHub repo), Git ডিফল্ট নাম দেয় origin।
এটা local নাম, GitHub repo বা folder name নয়।

Its is the name of remote repo in locallly
```

## Git push

![image.png](Git%20Learning/image%207.png)

## Synchonization

Merge locan and remote rep

![image.png](Git%20Learning/image%208.png)

## Git Pull

![image.png](Git%20Learning/image%209.png)

## Git Pull rebase

```jsx
https://chatgpt.com/share/69bad5ad-592c-8005-ac8a-d5e0f2b501d2
```

## Git Checout

```jsx
## git brach change
>> git checkout branch_name

## Create and checkout
>>git checkout -b feature-branch

##============   checkout to a commit ===============

>> git checkout 3a1b2c7(commit hash)

"""কি হয় যখন checkout করা হয়:
আপনার প্রজেক্টের সব ফাইল সেই কমিটের অবস্থা অনুযায়ী পরিবর্তিত হবে।
আপনি বর্তমানে কোনো “branch” এ থাকছেন না; এটা হয় detached HEAD অবস্থায়।
কেন ব্যবহার করা হয়:
কোনো পুরনো কমিট পরীক্ষা করতে।
পুরনো ফাইলগুলো থেকে কোড কপি বা তুলনা করতে।
ব্যাকআপ বা ডিবাগ করার জন্য।
💡 সতর্কতা: Detached HEAD অবস্থায় কাজ করলে, নতুন কমিট করলে তা branch-এ যোগ হবে না। যদি সেটা রাখতে চান, নতুন branch বানাতে হবে:
git checkout -b new-branch"""

##===================== Restore a specific file to the last committed version=========
>> git checkout -- <file-name>

"""
Restore a specific file to the last committed version” মানে হলো কোনো ফাইলকে শেষ কমিট করা অবস্থায় ফিরিয়ে আনা, যেই ফাইলের উপর আপনি পরিবর্তন করেছেন কিন্তু এখনো commit করেননি
কী ঘটে:
<file-name> ফাইলটি শেষ কমিট করা অবস্থায় ফিরে আসে।
ফাইলের সব uncommitted পরিবর্তন মুছে যায়।
কোনো branch বা commit পরিবর্তন হয় না, শুধুমাত্র ফাইলের অবস্থা বদল হয়।"""

## ====================== Discard all changes in the working directory=============
>> git checkout .

""" 
Discard all changes in the working directory” মানে হলো আপনার প্রজেক্টের সব ফাইলকে শেষ কমিট করা অবস্থায় ফিরিয়ে আনা, এবং যেকোনো uncommitted পরিবর্তন মুছে ফেলা।
কি ঘটে:
সব tracked ফাইল (যেগুলো Git ট্র্যাক করে) শেষ committed অবস্থায় ফিরে আসে।
আপনার করা যেকোনো uncommitted পরিবর্তন নষ্ট হয়ে যাবে।
নতুন ফাইল (যেগুলো add করা হয়নি) এর উপর কোনো প্রভাব নেই """

```

## Git Merge abort

### **Aborting a Merge**

If during this process you feel the merge is incorrect or too complex, you can cancel it.

- Stops the merge process.
- Restores previous branch state.
- Useful when conflicts are confusing.

```
git merge --abort
```

# **Git Rebase**

Git rebase integrates changes by replaying your commits on top of the latest state of another branch, creating a cleaner, linear history.

- Moves commits to the tip of the target branch.
- Rewrites commit history instead of creating a merge commit.
- Keeps project history linear and easier to read

```
git checkout <feature-branch>
git rebase <base-branch>
```

- **<feature-branch>** is the branch with the changes you want to rebase.
- **<base-branch>** is the branch you want to rebase your changes onto, typically main or master.

**Understanding the Rebase Process**

Rebase reapplies your commits on top of the latest base branch to create a clean, linear history.

- Git temporarily removes your changes.
- Then it adds the latest work from the branch you rebased onto.
- Then it reapplies your changes on top of that.
- It creates new commit versions of your changes (called C′ and D′).

### **Visual Working of Git Rebase**

![image.png](Git%20Learning/image%2010.png)

```
C1 → C2 → C3 → C4 → C5   (Main Branch)
        \
         B1 → B2 → B3     (Feature Branch)
```

- C1 to C5 are commits on the main branch (green).
- B1 to B3 are commits on the feature branch (yellow), created from C3 .
- At this point, the feature branch is outdated, because the main branch has moved ahead ( C4 and C5 were added after C3).

## Fork and Clone

![image.png](Git%20Learning/image%2011.png)

## Git diff

git diff is a Git command used to compare changes between different states of a repository, helping developers see what has been modified in files before committing or merging.

```jsx
## show diff between stage area and local 
>> git diff

## show diff between stage ares and last commit
>> git diff --staged

## show diff in a file
>> git diff file.txt

## show diff between two branch/commit
>> git diff commit1/branch1 commit2/branch2
```

## Git Undo

```jsx
# Hard undo: remove commit with last commit code
>> git reset --hard HEAD~1

## soft unod : remove only last commit
>> git reset --soft HEAD~1
```

![image.png](Git%20Learning/image%2012.png)

## Git logs

```jsx
## All commit details
>> git log

## All commmit in one line
>> git log --oneline

## All commit of a author
>> git log --author="mizan"

## serch commit
>> git log --grep="bug fix"

## view file change in every commit
>> git log --stat
```